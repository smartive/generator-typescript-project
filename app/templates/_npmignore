# Logs
logs
*.log
npm-debug.log*

# Dependency directory
node_modules

# Typescript stuff
build/
coverage/
config/
tsconfig.json
tslint.json

# Typescript files (but not the definitions)
*.ts
!*.d.ts

# Testfiles
jest.json
test/

<% if(provideGitlab || provideTravis || provideAppveyor) { %>
# CI Configs
<% if(provideGitlab) { %>.gitlab-ci.yml<% } %>
<% if(provideTravis) { %>.travis.yml<% } %>
<% if(provideAppveyor) { %>.appveyor.yml<% } %>
<% } %>

<% if(withTypedoc) { %>
# Documentation
docs/
<% } %>

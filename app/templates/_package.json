{
  "name": "<%= name %>",
  "version": "0.0.0-development",
  "description": "<%= description %>",
  "main": "<% if(type === 'library') { %>index<% } else { %>app<% } %>.js",<% if(type === 'library') { %>
  "typings": "index.d.ts",<% } %>
  "scripts": {
    "clean": "del-cli ./build ./coverage",
    "build": "npm run clean && tsc -p ./config/tsconfig.build.json",
    "develop": "npm run clean && tsc -p .",
    "lint": "tslint -c ./tslint.json -p ./config/tsconfig.build.json",
    "test": "npm run lint && npm run clean && jest -c ./jest.json",
    "test:watch": "npm run clean && jest -c ./jest.json --watch",
    "semantic-release": "semantic-release"<% if(withTypedoc) {%>,
    "typedoc": "del-cli ./docs && typedoc --ignoreCompilerErrors --out ./docs --mode file --tsconfig ./config/tsconfig.build.json ./src/"<% } %>
  },
  "keywords": [],
  "repository": {
    "type": "git",
    "url": "<%= gitUrl %>.git"
  },
  "bugs": {
    "url": "<%= gitUrl %>/issues"
  },
  "homepage": "<%= gitUrl %>#readme",
  "engines": {
    "node": "^9.0.0",
    "npm": "*"
  },
  "author": "<%= gitUser %> <<%= gitEmail %>>",
  "license": "ISC"<% if(scopedPackage) {%>,
  "publishConfig": {
    "access": "public"
  }<% } %>
}
